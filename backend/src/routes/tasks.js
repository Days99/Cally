const express = require('express');
const router = express.Router();

// Placeholder routes - to be implemented in Phase 4-5
router.get('/status', (req, res) => {
  res.json({ message: 'Tasks routes - Coming soon!' });
});

module.exports = router; 